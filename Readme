#Описание алгоритма фильтрации строк#
Этот C# код представляет собой консольное приложение, которое фильтрует введенные строки.
И выводит те из них, длина которых не превышает 3 символов.
## Использование
1. Запустите программу.
2. Введите строки через запятую.
3. Программа выведет строки, длина которых не превышает 3 символов.
## Алгоритм решения
1. Ввод данных:
   - Пользователь вводит строки через запятую.
   - Входная строка разбивается на массив строк.

2. Фильтрация строк:
   - Проход по каждой строке в массиве.
   - Если длина строки не превышает 3 символов, она добавляется в новый массив.

3. Создание окончательного массива:
   - Создается новый массив с отфильтрованными строками.
   - Копирование подходящих строк из временного массива в окончательный массив.

4. Вывод результата:
   - Вывод каждой строки из окончательного массива.

## Пример

**Ввод** :Введите строки через запятую:
apple, cat, dog, tree, car


Вывод:
Результат:
cat
dog
car